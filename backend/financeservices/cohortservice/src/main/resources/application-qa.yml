server:
  port: 8080

spring:
  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Kolkata
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: false
  datasource:
    url: "${DB_URL:jdbc:postgresql://finance-db-qa.us-east-1.rds.amazonaws.com:5432/mydb}"
    driver-class-name: org.postgresql.Driver

springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    org:
      springframework: INFO
    com:
      nous:
        cohortservice: INFO
